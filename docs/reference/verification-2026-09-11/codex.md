# Codex OAuth — verified 2026-09-11

Day-one wire-fact verification for a macOS Rust/Tauri desktop app that signs in with the user's ChatGPT subscription via the Codex CLI OAuth flow and calls the Codex backend directly. Every value below was read today from the cited primary source. Nothing is filled from memory; gaps are marked UNVERIFIED.

Method: `git clone --depth 1` of the four reference repos into `/agent/workspace/research/refs/` (SSL verification had to be disabled for the sandbox's intercepting proxy; content integrity was not otherwise affected), grep/read of source; GitHub REST API for release metadata; Exa page fetches for docs pages the sandbox could not reach directly (registry.npmjs.org is blocked by network policy in this sandbox).

## Sources consulted

| Handle | Source | Commit / version | Fetched | Used for |
|---|---|---|---|---|
| `codex` | https://github.com/openai/codex (Rust CLI monorepo; paths below are relative to `codex-rs/` unless noted) | commit `ab95cd4dd9c9c65558cb9b6042c463b0fc66715d` (2026-09-11 06:56 UTC, "Preserve voice caption order…") | 2026-09-11 | A–G: `login/src/server.rs`, `login/src/pkce.rs`, `login/src/device_code_auth.rs`, `login/src/token_data.rs`, `login/src/success_page.rs`, `login/src/auth/manager.rs`, `login/src/auth/storage.rs`, `login/src/auth/default_client.rs`, `protocol/src/auth.rs`, `protocol/src/account.rs`, `protocol/src/openai_models.rs`, `protocol/src/models.rs`, `model-provider-info/src/lib.rs`, `model-provider/src/auth.rs`, `model-provider/src/bearer_auth_provider.rs`, `model-provider/src/provider.rs`, `codex-api/src/common.rs`, `codex-api/src/provider.rs`, `codex-api/src/endpoint/responses.rs`, `codex-api/src/endpoint/models.rs`, `codex-api/src/requests/headers.rs`, `codex-api/src/rate_limits.rs`, `codex-api/src/api_bridge.rs`, `codex-api/src/sse/responses.rs`, `core/src/client.rs`, `core/src/responses_metadata.rs`, `core/src/attestation.rs`, `core/src/image_preparation.rs`, `core/src/installation_id.rs`, `models-manager/src/manager.rs`, `models-manager/src/lib.rs`, `models-manager/models.json`, `config/src/types.rs`, `cli/src/main.rs`, `cli/src/login.rs`, `cli/src/doctor.rs`, `tui/src/onboarding/auth.rs`, `../docs/authentication.md`, `../.github/workflows/rust-release.yml`, `../.github/workflows/rust-release-prepare.yml` |
| `codex-rel` | GitHub REST API `repos/openai/codex/releases/latest` and `/releases?per_page=8` and `/releases/tags/rust-v0.154.0` | latest stable `rust-v0.154.0` published 2026-09-09T22:35:38Z; newest prerelease `rust-v0.155.0-alpha.3` 2026-09-11T02:35:52Z | 2026-09-11 | E (current client_version), release notes |
| `oo` | https://github.com/EvanZhouDev/openai-oauth (TypeScript) | commit `ec7dab2fcd8dab9da970a7a2b5dc34046c94905e` (2026-07-15) | 2026-09-11 | A–E, H: `packages/core/src/runtime.ts`, `packages/core/src/models.ts`, `packages/local/src/auth-file.ts`, `packages/openai-oauth/src/login.ts`, `packages/openai-oauth/src/responses.ts`, `packages/openai-oauth/src/models.ts`, `README.md` |
| `lwc` | https://github.com/opencoredev/login-with-chatgpt (source of npm `@opencoredev/loginwithchatgpt-core`) | commit `3befb7fb625170cb305b116a654c7e2f8672bae4` (2026-08-01). npm page (https://www.npmjs.com/package/@opencoredev/loginwithchatgpt-core, fetched via Exa 2026-09-11): latest `0.2.0` published 2026-07-08T15:30:55Z, MIT | 2026-09-11 | A–E: `packages/core/src/constants.ts`, `oauth.ts`, `device.ts`, `tokens.ts`, `jwt.ts`, `pkce.ts`, `config.ts`, `codex-transport.ts` |
| `cpa` | https://github.com/router-for-me/CLIProxyAPI (Go) | commit `09a29bd345bc44c473abe7fd07859e32df2ea543` (2026-09-10) | 2026-09-11 | A–F: `internal/auth/codex/openai_auth.go`, `pkce.go`, `token.go`, `jwt_parser.go`, `errors.go`, `oauth_server.go`, `sdk/auth/codex_device.go`, `internal/runtime/executor/codex_executor_request.go`, `codex_executor_execute.go`, `codex_executor_auth.go`, `codex_executor_terminal.go`, `codex_websockets_request.go`, `internal/registry/codex_client_models_updater.go` |
| `oai-auth-docs` | https://developers.openai.com/codex/auth (official Codex auth docs; `codex/docs/authentication.md` just links here) | fetched via Exa 2026-09-11 (cached crawl) | 2026-09-11 | G (credential store modes), H |
| `oai-help` | https://help.openai.com/en/articles/11369540-codex-in-chatgpt-faq | fetched via Exa 2026-09-11 ("Updated: 5 hours ago") | 2026-09-11 | H |
| `openclaw-docs` | https://docs.openclaw.ai/concepts/oauth and https://docs.openclaw.ai/providers/openai | live crawl 2026-09-11 | 2026-09-11 | H (third-party claim of OpenAI sanction) |
| `news` | The Next Web 2026-05-03, Storyboard18 2026-05-04, News9live 2026-05-04, The Verge 2026-05-15, TechTimes 2026-08-03; developer.puter.com 2026-06-11; gist ravidsrk 2026-06-20; community.openai.com threads 1371778 (2026-01-16), 1380525 (2026-05-08), 1389585 (2026-08-08) | Exa search 2026-09-11 | 2026-09-11 | H |

Path legend in tables: `codex:login/src/server.rs:576` = repo `codex` at commit `ab95cd4`, file `codex-rs/login/src/server.rs`, line 576.

## A. OAuth

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Client ID | `app_EMoamEEZ73f0CkXaXp7hrann` | `codex:login/src/auth/manager.rs:1724` (`pub const CLIENT_ID`); same literal in `lwc:constants.ts`, `oo:runtime.ts` (`DEFAULT_OPENAI_OAUTH_CLIENT_ID`), `cpa:openai_auth.go` | 2026-09-11 | CLI allows override via env `CODEX_APP_SERVER_LOGIN_CLIENT_ID` (`manager.rs:204`, `oauth_client_id()`). Matches brief. |
| Issuer | `https://auth.openai.com` | `codex:login/src/server.rs:59` (`DEFAULT_ISSUER`) | 2026-09-11 | |
| Authorize URL | `{issuer}/oauth/authorize` = `https://auth.openai.com/oauth/authorize` | `codex:login/src/server.rs:611` | 2026-09-11 | |
| Authorize query params (in CLI order) | `response_type=code`, `client_id=<id>`, `redirect_uri=<uri>`, `scope=<scope>`, `code_challenge=<S256 challenge>`, `code_challenge_method=S256`, `id_token_add_organizations=true`, `codex_cli_simplified_flow=true`, `state=<32 random bytes, base64url no pad>`, `originator=<originator, default codex_cli_rs>`; optional `allowed_workspace_id=<comma-joined ids>` when `forced_chatgpt_workspace_id` is configured | `codex:login/src/server.rs:576-612` (`build_authorize_url`), `server.rs:614-618` (`generate_state`); expected full URL literal in test `codex:tui/src/onboarding/auth.rs:1110-1123` | 2026-09-11 | Values are `urlencoding::encode`d (spaces in scope become `%20`). `oo:runtime.ts` explicitly refuses to send `originator` from `extraParams`; `cpa` adds `prompt=login` and omits `originator`; `lwc` sends `originator`. |
| Exact scope string | `openid profile email offline_access api.connectors.read api.connectors.invoke` | `codex:login/src/server.rs:590-591`; test literal `tui/src/onboarding/auth.rs:1114-1115` | 2026-09-11 | DELTA vs brief (brief has 4 scopes). All three reference impls still use `openid profile email offline_access` (`oo:runtime.ts` `DEFAULT_OPENAI_OAUTH_SCOPE`; `lwc:constants.ts` `DEFAULT_SCOPE`; `cpa:openai_auth.go` `openid email profile offline_access`). The two `api.connectors.*` scopes are for Codex connectors; a chat-only app can presumably omit them, but that this still yields a working Codex backend token is UNVERIFIED by us (the refs' continued use is indirect evidence). |
| Redirect URI | `http://localhost:{port}/auth/callback`; default port `1455`; fallback port `1457` ("Keep in sync with the Codex CLI Hydra redirect URI allow-list") | `codex:login/src/server.rs:60-62,176,637-695` | 2026-09-11 | Bound to `127.0.0.1:{port}`; if 1455 is busy the CLI first sends `GET /cancel` to the previous instance, retries 10x200ms, then falls back to 1457 exactly once. Path `/auth/callback` is fixed. Other local paths served: `/success`, `/cancel`. Callback query: `code`, `state`, `error`, `error_description`. |
| PKCE | S256. Verifier = 64 random bytes base64url-no-pad (86 chars); challenge = base64url-no-pad(SHA-256(verifier)) | `codex:login/src/pkce.rs:12-27` | 2026-09-11 | `cpa` uses 96 bytes (128 chars); `oo` 48 bytes; `lwc` 48 bytes. Any 43–128 char verifier is RFC-valid. |
| Token endpoint | `{issuer}/oauth/token` = `https://auth.openai.com/oauth/token` | `codex:login/src/server.rs:827`; `manager.rs:200` (`REFRESH_TOKEN_URL`) | 2026-09-11 | Env override for refresh only: `CODEX_REFRESH_TOKEN_URL_OVERRIDE`. Revoke endpoint also exists: `https://auth.openai.com/oauth/revoke` (`manager.rs:201`). |
| Code exchange request | `POST` with `Content-Type: application/x-www-form-urlencoded`; body `grant_type=authorization_code&code=<code>&redirect_uri=<redirect_uri>&client_id=<client_id>&code_verifier=<verifier>` | `codex:login/src/server.rs:834-845` | 2026-09-11 | Same encoding in `oo`, `lwc`, `cpa` (`cpa` also sends `Accept: application/json`). |
| Code exchange response fields read by CLI | `id_token`, `access_token`, `refresh_token` (all three required; deserialization fails otherwise) | `codex:login/src/server.rs:817-822,876` | 2026-09-11 | `cpa`/`oo` also read `token_type`, `expires_in` (UNVERIFIED by us that `expires_in` is present). |
| Post-login API-key exchange (browser flow only) | Second `POST {issuer}/oauth/token`, form-encoded: `grant_type=urn:ietf:params:oauth:grant-type:token-exchange&client_id=<id>&requested_token=openai-api-key&subject_token=<id_token>&subject_token_type=urn:ietf:params:oauth:token-type:id_token`; response `access_token` is stored in `auth.json` `OPENAI_API_KEY` | `codex:login/src/server.rs:1137-1172` (`obtain_api_key`), called at `server.rs:429-436` with `.ok()` (failure ignored) | 2026-09-11 | Not done in the device-code flow (`device_code_auth.rs:224` passes `None`). Optional for a third-party app. |
| Refresh request | `POST https://auth.openai.com/oauth/token` with `Content-Type: application/json`; JSON body `{"client_id":"<id>","grant_type":"refresh_token","refresh_token":"<rt>"}` (no `scope`) | `codex:login/src/auth/manager.rs:1599-1617` (`request_chatgpt_token_refresh`), struct `RefreshRequest` at `manager.rs:1709-1714` | 2026-09-11 | Note the asymmetry: code exchange is form-encoded, refresh is JSON. `oo:runtime.ts` also uses JSON without scope; `lwc:oauth.ts` uses JSON with `scope`; `cpa:openai_auth.go` uses form-encoding with `scope=openid profile email` — so the endpoint reportedly accepts both encodings (form-encoded refresh UNVERIFIED by us). |
| Refresh response fields | `id_token`, `access_token`, `refresh_token` (each optional; CLI keeps old values for missing ones) | `codex:login/src/auth/manager.rs:1716-1721,1582-1592` | 2026-09-11 | Refresh tokens rotate: `oo`/`lwc` fall back to the previous refresh token when none is returned. |
| Refresh failure codes | Body `error` (string) or `error.code`, or top-level `code`: `refresh_token_expired` → Expired, `refresh_token_reused` → Exhausted, `refresh_token_invalidated` → Revoked; HTTP 400 + `invalid_grant` or any HTTP 401 → permanent failure; other statuses → transient | `codex:login/src/auth/manager.rs:1626-1707` | 2026-09-11 | `cpa` treats `refresh_token_reused` as non-retryable; `lwc` treats all four codes as dead. |
| Refresh cadence rule | Proactively refresh when access-token JWT `exp` ≤ now + 5 minutes; if `exp` is absent, refresh when `last_refresh` < now − 8 days | `codex:login/src/auth/manager.rs:191-192` (`TOKEN_REFRESH_INTERVAL = 8`, `CHATGPT_ACCESS_TOKEN_REFRESH_WINDOW_MINUTES = 5`), `manager.rs:2959-2981` (`should_refresh_proactively`) | 2026-09-11 | `oo:auth-file.ts` uses exp−5min or `last_refresh` older than 55 min; `lwc:tokens.ts` uses exp−60s. |
| 401 recovery | On HTTP 401 from the backend the CLI runs `UnauthorizedRecovery`: step 1 reload `auth.json` (only if `chatgpt_account_id` unchanged), step 2 refresh token, then retry once; 403 is not treated as recoverable | `codex:model-provider/src/provider.rs:195-200` (`is_recoverable_auth_error` = status 401 only); `codex:login/src/auth/manager.rs:1834-1839,1888-2036` | 2026-09-11 | Account mismatch message: `REFRESH_TOKEN_ACCOUNT_MISMATCH_MESSAGE` (`manager.rs:199`). |
| Device-code flow: step 1 | `POST {issuer}/api/accounts/deviceauth/usercode` = `https://auth.openai.com/api/accounts/deviceauth/usercode`, `Content-Type: application/json`, body `{"client_id":"app_EMoamEEZ73f0CkXaXp7hrann"}`; response `{"device_auth_id":..., "user_code":... (alias "usercode"), "interval": "<seconds as string>"}` | `codex:login/src/device_code_auth.rs:27-34,62-97,165-179`; same URLs in `cpa:sdk/auth/codex_device.go:27-30`, `lwc:config.ts` | 2026-09-11 | HTTP 404 → "device code login is not enabled for this Codex server". `interval` is deserialized from a **string**; `cpa`/`lwc` accept string or int and default to 5 s. Actual server value UNVERIFIED. |
| Device-code flow: user-facing URL | `https://auth.openai.com/codex/device` (user enters `user_code`; "expires in 15 minutes") | `codex:login/src/device_code_auth.rs:174,155` | 2026-09-11 | Matches brief's URL, but note it is the verification page, not the API endpoint. |
| Device-code flow: step 2 (poll) | `POST https://auth.openai.com/api/accounts/deviceauth/token`, JSON body `{"device_auth_id":..., "user_code":...}`; HTTP 403 or 404 → keep polling every `interval` s; max wait 15 min; success body `{"authorization_code":..., "code_challenge":..., "code_verifier":...}` | `codex:login/src/device_code_auth.rs:41-45,55-60,99-147` | 2026-09-11 | `lwc:device.ts` also treats 429 as "pending" (Cloudflare). The server generates the PKCE pair for this flow. |
| Device-code flow: step 3 | Standard code exchange (form-encoded, as above) with `redirect_uri=https://auth.openai.com/deviceauth/callback` and the server-supplied `code_verifier` | `codex:login/src/device_code_auth.rs:198-213`; `cpa:codex_device.go:30`; `lwc:config.ts` `deviceRedirectUri` | 2026-09-11 | Device flow returns an authorization code that is then exchanged — it does NOT return tokens directly. CLI flag: `codex login --device-auth` (`cli/src/main.rs:523`). |
| OAuth callback error handling | `error=access_denied` with `error_description` containing `missing_codex_entitlement` → "Codex is not enabled for your workspace…"; `state` mismatch → HTTP 400 "State mismatch" | `codex:login/src/server.rs:346-402,988-1011` | 2026-09-11 | |
| Auth-endpoint HTTP client | Auth requests use a "raw" client **without** the Codex default `originator`/`User-Agent` headers | `codex:login/src/auth/default_client.rs:313-321` (`create_raw_auth_client`) | 2026-09-11 | Refresh uses the shared client with default headers (`manager.rs:1610` comment). |

## B. Identity

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Claim namespace | `https://api.openai.com/auth` (object) in both ID token and access token | `codex:login/src/token_data.rs:77`, `login/src/success_page.rs:106-139` (`jwt_auth_claims`, applied to both tokens) | 2026-09-11 | JWT payload decoded base64url-no-pad; signature is NOT verified by the CLI. |
| Claims read under the namespace | `chatgpt_account_id` (string), `chatgpt_plan_type` (string), `chatgpt_user_id` (string; fallback `user_id`), `chatgpt_account_is_fedramp` (bool), `chatgpt_account_user_id` (string), plus `organization_id`, `project_id`, `completed_platform_onboarding` (bool), `is_org_owner` (bool) used only for the post-login success page | `codex:login/src/token_data.rs:87-117,174-198`; `login/src/success_page.rs:48-66,77-81` | 2026-09-11 | `cpa:jwt_parser.go` additionally models `chatgpt_subscription_active_start`, `chatgpt_subscription_active_until`, `chatgpt_subscription_last_checked`, `groups`, `organizations[]{id,is_default,role,title}` under the same namespace, and top-level `at_hash, aud, auth_provider, auth_time, email, email_verified, exp, iat, iss, jti, rat, sid, sub`. Presence of these extra claims UNVERIFIED by us. |
| Where `account_id` comes from | ID token → `https://api.openai.com/auth`.`chatgpt_account_id`, copied into `auth.json` `tokens.account_id` at login; at request time `CodexAuth::get_account_id()` returns `tokens.account_id` | `codex:login/src/server.rs:898-909` (`persist_tokens_async`); `login/src/auth/manager.rs:587-599` | 2026-09-11 | `oo:runtime.ts` `deriveAccountId` falls back to top-level `chatgpt_account_id`, then `organizations[0].id`, and tries the access token too. |
| Plan type source | CLI stores plan from the **ID token**; the success page reads `chatgpt_plan_type` from the **access token** claims | `codex:login/src/token_data.rs:174-187`; `login/src/success_page.rs:77-81` | 2026-09-11 | Both tokens carry the namespace. |
| Known plan values | `free`, `go`, `plus`, `pro`, `prolite`, `team`, `self_serve_business_prolite`, `self_serve_business_usage_based`, `business`, `ent26`, `enterprise_cbp_automation`, `enterprise_cbp_usage_based`, `enterprise` (alias `hc`), `edu` (alias `education`), `edu_plus`, `edu_pro`; anything else → Unknown | `codex:protocol/src/auth.rs:66-126` | 2026-09-11 | Display names in `auth.rs:128-148`. |
| Display claims | `email`: top-level `email`, else `https://api.openai.com/profile`.`email`. `name`: not read by the CLI | `codex:login/src/token_data.rs:71-85,176-178` | 2026-09-11 | `lwc:jwt.ts` `parseUser` reads top-level `name` — presence of `name` in the ID token UNVERIFIED. |
| Access-token expiry | Read from standard `exp` claim | `codex:login/src/token_data.rs:101-105,142-147` | 2026-09-11 | Actual lifetime UNVERIFIED (see below). |

## C. Backend requests

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Base URL | `https://chatgpt.com/backend-api/codex` (used whenever auth mode is ChatGPT/ChatgptAuthTokens/Headers/AgentIdentity/PersonalAccessToken); `https://api.openai.com/v1` for API-key mode | `codex:model-provider-info/src/lib.rs:43,370-388` (`CHATGPT_CODEX_BASE_URL`) | 2026-09-11 | Alternate hosts recognised by `codex:agent-identity/src/lib.rs:61-69`: `https://chat.openai.com/backend-api/codex` (production alias), `https://chatgpt-staging.com/backend-api/codex` (staging). Admins may pin `chatgpt_base_url` (`oai-auth-docs`). |
| Responses URL | `POST {base}/responses` = `https://chatgpt.com/backend-api/codex/responses` | `codex:codex-api/src/endpoint/responses.rs:40-46,167-181`; literal in `cli/src/doctor.rs:3675` | 2026-09-11 | Sibling routes: `/guardian`, `/guardian-classifier`, `/responses/compact` (`cpa`), `/models`, `/alpha/search`, `/analytics-events/events`. WebSocket variant of `/responses` exists (`wss://`), gated by `OpenAI-Beta: responses_websockets=2026-02-06`. |
| `Authorization` | `Bearer <access_token>` | `codex:model-provider/src/bearer_auth_provider.rs:35-39` | 2026-09-11 | |
| `chatgpt-account-id` | `<tokens.account_id>` (source literal `"ChatGPT-Account-ID"`; the `http` crate lowercases header names on the wire) | `codex:model-provider/src/bearer_auth_provider.rs:40-44`; `model-provider/src/auth.rs:317-325` | 2026-09-11 | Sent on every Codex-backend request including `/models`. |
| `X-OpenAI-Fedramp` | `true` — only when `chatgpt_account_is_fedramp` is true | `codex:model-provider/src/bearer_auth_provider.rs:45-47` | 2026-09-11 | `oo:runtime.ts` mirrors this. |
| `OpenAI-Beta` | **Not sent on the HTTP `/responses` path.** Only the WebSocket transport sends `OpenAI-Beta: responses_websockets=2026-02-06` | `codex:core/src/client.rs:151,166,1175-1178` (WS handshake only); `cli/src/doctor.rs:2287-2290` (WS probe); repo-wide grep for `responses=experimental` returns zero hits | 2026-09-11 | DELTA vs brief (`responses=experimental`). `lwc:codex-transport.ts` still sends `OpenAI-Beta: responses=experimental`; `oo:runtime.ts` explicitly deletes `openai-beta`. Whether the server rejects/ignores the stale value is UNVERIFIED. |
| `originator` | Default `codex_cli_rs`; `codex_exec` for `codex exec`; app-server clients set their own (TUI reports client name `codex-tui`). First-party set: `codex_cli_rs`, `codex-tui`, `codex_vscode`, `Codex *`; chat originators `codex_atlas`, `codex_chatgpt_desktop` | `codex:login/src/auth/default_client.rs:40,153-162,335-338`; `exec/src/lib.rs:260`; `app-server/src/request_processors/initialize_processor.rs:104,149` | 2026-09-11 | Env override `CODEX_INTERNAL_ORIGINATOR_OVERRIDE`. `cpa` impersonates `codex-tui`. |
| `User-Agent` | `{originator}/{version} ({os_type} {os_version}; {arch}) {terminal_token}` + optional ` ({suffix})`. Example built by OpenAI's own release workflow: `codex_cli_rs/99.99.99 (Linux <kernel>; x86_64) github-actions`. `cpa`'s hard-coded imitation: `codex-tui/0.153.3 (Mac OS 26.5.1; arm64) iTerm.app/3.6.11 (codex-tui; 0.153.3)` | `codex:login/src/auth/default_client.rs:164-188`; `codex:../.github/workflows/rust-release-prepare.yml:38`; `cpa:codex_executor_request.go:27` | 2026-09-11 | Terminal token from `codex-terminal-detection` (`TERM_PROGRAM`/`TERM_PROGRAM_VERSION`, e.g. `iTerm.app/3.6.11`). |
| `version` | `<CARGO_PKG_VERSION>` (e.g. `0.154.0`) — added as a provider `http_headers` entry on the built-in OpenAI provider | `codex:model-provider-info/src/lib.rs:474-478` | 2026-09-11 | Env-driven optional headers `OpenAI-Organization`/`OpenAI-Project` (`lib.rs:479-489`). |
| `Accept` | `text/event-stream` (streaming) | `codex:codex-api/src/endpoint/responses.rs:174-178` | 2026-09-11 | `cpa` sends `application/json` for non-stream compaction. |
| `Content-Type` | `application/json` (request body optionally zstd-compressed via `Compression::Zstd`) | `codex:codex-api/src/endpoint/responses.rs:115,162-165`; `codex-api/src/requests/mod.rs` | 2026-09-11 | Default compression is `None`. |
| Session headers | `session-id: <session_id>`, `thread-id: <thread_id>`, `x-client-request-id: <thread_id>` | `codex:codex-api/src/requests/headers.rs:5-14`; `codex-api/src/endpoint/responses.rs:118-122` | 2026-09-11 | DELTA vs brief (`session_id`, `conversation_id`). `cpa` still special-cases legacy `Session_id`/`Conversation_id` names, implying older CLI versions used them. `session_id` is the string also used as `prompt_cache_key`. |
| Other Codex headers | `x-codex-window-id: <window_id>` (always), `x-codex-turn-metadata: <JSON>` (compat projection), `x-codex-parent-thread-id`, `x-openai-subagent: review|compact|memory_consolidation|collab_spawn|<label>`, `x-codex-beta-features: <comma list>`, `x-codex-turn-state: <opaque token echoed from response header of same name>`, `x-codex-routing-hint`, `x-oai-attestation` (only when provider `supports_attestation()` and a host `AttestationProvider` exists), `x-openai-internal-codex-responses-lite: true` (Responses-Lite models), `x-openai-memgen-request: true`, `x-responsesapi-include-timing-metrics: true`, `x-openai-internal-codex-residency: us` (only if residency requirement configured) | `codex:core/src/client.rs:151-168,2053-2081`; `core/src/responses_metadata.rs:354-382`; `core/src/attestation.rs:7`; `core/src/client.rs:454,732-740`; `login/src/auth/default_client.rs:42,341-349` | 2026-09-11 | None of these are demonstrably required for a plain chat client; `x-codex-turn-state` should be echoed if received (sticky routing). |
| Body: fields serialized | `model` (string slug), `instructions` (omitted when empty), `input` (array of ResponseItem), `tools` (omitted if none), `tool_choice: "auto"`, `parallel_tool_calls` (bool), `reasoning: {effort?, summary?, context?}`, `store: false`, `stream: true`, `stream_options: {reasoning_summary_delivery:"sequential_cutoff"}` (optional), `include: ["reasoning.encrypted_content"]`, `service_tier` (optional), `prompt_cache_key: <session_id>`, `text: {verbosity?, format?}` (optional), `client_metadata: {…}` (optional), `access_programs` (optional) | `codex:codex-api/src/common.rs:259-285` (`ResponsesApiRequest`); values set in `core/src/client.rs:852-889` (`build_responses_request`) | 2026-09-11 | `previous_response_id` exists only on the WebSocket struct and is always `None` (`common.rs:292`). `oo:responses.ts` rejects `previous_response_id`/`item_reference` ("Stateless Codex responses endpoint…"); `lwc` strips server ids and `item_reference`, deletes `max_output_tokens`/`max_completion_tokens`; `cpa` deletes `previous_response_id`, `generate`, `prompt_cache_retention`, `safety_identifier`, `stream_options`. |
| `reasoning` wire values | `effort`: `none|low|medium|high|xhigh` (lowercase); `summary`: `auto|concise|detailed|none` (per `ReasoningSummary` config); `context`: `auto|current_turn|all_turns` (`all_turns` used with Responses-Lite) | `codex:codex-api/src/common.rs:148-164`; `lwc:codex-transport.ts` `ReasoningEffort`; `oo:runtime.ts` `applyModelDefaults` | 2026-09-11 | `lwc` comment: "reasoning must be configured (Codex models always reason)". |
| `text` wire values | `verbosity`: `low|medium|high`; `format`: `{type:"json_schema", strict, schema, name:"codex_output_schema"}` | `codex:codex-api/src/common.rs:177-223,374-392` | 2026-09-11 | |
| `client_metadata` keys | `x-codex-installation-id`, `session_id`, `thread_id`, `x-codex-window-id`, `turn_id`, `x-openai-subagent`, `x-codex-parent-thread-id`, `parent_turn_id`, `root_turn_id`, `x-codex-turn-metadata` (JSON string) | `codex:core/src/responses_metadata.rs:315-352` | 2026-09-11 | `installation_id` is a UUIDv4 persisted at `$CODEX_HOME/installation_id` (`core/src/installation_id.rs:17,54`). Optional. |
| `instructions` content | The CLI sends the model's `model_messages.instructions_template` (a ~13–21 KB Codex system prompt, per model) as `instructions`; for `use_responses_lite` models it instead sends `instructions: ""` and prepends the prompt as a `developer` message plus an `additional_tools` item in `input` | `codex:core/src/client.rs:794-835`; `protocol/src/openai_models.rs:534-552` (`get_model_instructions`); template lengths measured in `models-manager/models.json` | 2026-09-11 | The instruction text comes from the `/models` catalog itself (legacy field `base_instructions`, promoted into `model_messages.instructions_template` on deserialize — `openai_models.rs:811-853`); OpenAI's own release workflow strips `base_instructions` before bundling (`rust-release-prepare.yml:46`). |
| Is the Codex prompt required server-side? | No client-side evidence of a requirement; reference impls send `""` (`oo`, `cpa`) or a one-sentence custom prompt (`lwc`) and report working streams | `oo:runtime.ts` `DEFAULT_CODEX_INSTRUCTIONS = ""` and `oo:README.md` "What is Supported"; `cpa:codex_executor_request.go` `normalizeCodexInstructions` (null → `""`), test `codex_executor_instructions_test.go`; `lwc:constants.ts` `DEFAULT_CODEX_INSTRUCTIONS` | 2026-09-11 | Contradicted by a secondary source (`news`: developer.puter.com 2026-06-11 claims OpenAI's check "validates a Codex-specific system prompt"). Server behavior UNVERIFIED — see UNVERIFIED list. The `lwc` transport comment attributes empty-output failures to missing `reasoning`, missing `include`, server-side ids, or `max_output_tokens`, not to `instructions`. |
| Image input serialization | `{"type":"input_image","image_url":"data:<mime>;base64,<...>","detail":"high"}` inside a user message `content` array; `detail` values `auto|low|high|original`; default `high`; `low` is rejected client-side; remote `http(s)` URLs are rejected client-side (placeholder text substituted); images are re-encoded/resized by `codex_utils_image::load_data_url_for_prompt` | `codex:protocol/src/models.rs:878-889,916-925,2003-2010`; `core/src/image_preparation.rs:25-31,255-330` | 2026-09-11 | Concrete pixel/byte limits live in the `codex-utils-image` crate (not read) — "exceeded the supported size limit" placeholder exists; exact limit UNVERIFIED. `supports_image_detail_original` per model gates `original`. |
| Local dev sandbox note | Provider retry defaults: 4 request retries, 5 stream retries, 300 s stream idle timeout, 429 not retried at transport layer | `codex:model-provider-info/src/lib.rs:29-31,391-397` | 2026-09-11 | |

## D. Streaming

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Transport | SSE (`data: <json>` lines), each event JSON has `type` | `codex:codex-api/src/sse/responses.rs:169-184` (`ResponsesStreamEvent`: `type`, `headers`, `metadata`, `response`, `item`, `item_id`, `call_id`, `delta`, `text`, `summary_index`, `content_index`, `safety_buffering`) | 2026-09-11 | |
| Events handled | `response.created` (→ `response.id`), `response.output_item.added` (`item`), `response.output_item.done` (`item`), `response.output_text.delta` (`delta`), `response.custom_tool_call_input.delta` (`delta`,`item_id`/`call_id`), `response.reasoning_summary_text.delta` (`delta`,`summary_index`), `response.reasoning_summary_text.done` (`item_id`,`text`,`summary_index`), `response.reasoning_text.delta` (`delta`,`content_index`), `response.reasoning_summary_part.added` (`summary_index`), `response.completed`, `response.incomplete` (→ error "Incomplete response returned, reason: <incomplete_details.reason>"), `response.failed` (→ error from `response.error`) | `codex:codex-api/src/sse/responses.rs:353-522` | 2026-09-11 | |
| Events explicitly ignored | `codex.response.metadata`, `response.content_part.added`, `response.content_part.done`, `response.custom_tool_call_input.done`, `response.function_call_arguments.delta`, `response.function_call_arguments.done`, `response.in_progress`, `response.metadata` (but see next row), `response.output_text.done`, `response.reasoning_summary_part.done`, `responsesapi.websocket_timing`, any other `*.delta` | `codex:codex-api/src/sse/responses.rs:523-538` | 2026-09-11 | Function-call arguments are taken from `response.output_item.done` items, not deltas. |
| `response.metadata` side-channels | `headers["x-codex-turn-state"]` (sticky routing token), `headers["openai-model"]`/`x-openai-model` (effective model), `metadata.openai_verification_recommendation`, `metadata.openai_chatgpt_moderation_metadata`, `metadata.type == "safety_buffering"` | `codex:codex-api/src/sse/responses.rs:220-262,278-300` | 2026-09-11 | |
| HTTP response headers read | `x-codex-turn-state`, `openai-model`, `x-reasoning-included`, `x-request-id`, `x-models-etag` (WS), all `x-codex-*` rate-limit headers (see F) | `codex:codex-api/src/sse/responses.rs:30-33,42,67`; `codex-api/src/endpoint/responses_websocket.rs:155-158` | 2026-09-11 | |
| `response.completed` usage fields | `response.id`; `response.usage: {input_tokens, input_tokens_details:{cached_tokens, cache_write_tokens?}, output_tokens, output_tokens_details:{reasoning_tokens}, total_tokens, codex_rollout_budget_units?}`; `response.usage_metadata?`; `response.end_turn?` (bool) | `codex:codex-api/src/sse/responses.rs:118-153` | 2026-09-11 | Full `usage` object also stashed into `usage_metadata.metadata`. |
| `response.failed` error codes mapped | `error.code`: `context_length_exceeded` → ContextWindowExceeded; `insufficient_quota` & friends → QuotaExceeded; `usage_not_included`; `cyber_policy`; `misalignment_policy_violation`; `invalid_prompt`/`bio_policy` → InvalidRequest; `server_is_overloaded`/`slow_down` → ServerOverloaded; `rate_limit_exceeded` → RateLimitExceeded (retry-after parsed from message); else Retryable | `codex:codex-api/src/sse/responses.rs:417-470` | 2026-09-11 | |
| WebSocket extras (not needed for HTTP) | Request envelope `{"type":"response.create", ...}`; server also emits `codex.rate_limits` events (`{"type":"codex.rate_limits","plan_type",...,"rate_limits":{"primary":{"used_percent","window_minutes","reset_at"},"secondary":{...}},"credits":{"has_credits","unlimited","balance"},"metered_limit_name"|"limit_name"}`) | `codex:codex-api/src/common.rs:366-372`; `codex-api/src/rate_limits.rs:104-169`; `codex-api/src/endpoint/responses_websocket.rs:773-774` | 2026-09-11 | |

## E. Models catalog

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Does the CLI call it? | Yes. `GET {base}/models?client_version=<MAJOR.MINOR.PATCH>`; result cached on disk at `$CODEX_HOME/models_cache.json`, keyed by client_version + auth identity, with `ETag` renewal | `codex:codex-api/src/endpoint/models.rs:31-79`; `models-manager/src/manager.rs:31,408-497,553-589`; `models-manager/src/lib.rs:19-26` (`client_version_to_whole`) | 2026-09-11 | Refresh strategies: `Online`, `Offline`, `OnlineIfUncached`. Called whenever auth uses the Codex backend (or API-key discovery is enabled). |
| Exact URL | `https://chatgpt.com/backend-api/codex/models?client_version=0.154.0` for the current stable release | URL builder `codex:codex-api/src/endpoint/models.rs:35-44` (test asserts `…/models?client_version=0.99.0`); version: `codex-rel` tag `rust-v0.154.0` (2026-09-09) and `rust-release.yml:34-52` enforcing tag == `Cargo.toml` version | 2026-09-11 | `client_version` is the prerelease-stripped `CARGO_PKG_VERSION`. OpenAI's own workflow fetches the full catalog with `client_version=99.99.99` (`rust-release-prepare.yml:36-46`). `oo:models.ts` resolves the latest version from the npm registry (fallback `0.144.1`); `lwc:constants.ts` hard-codes `0.142.5` with the comment that omitting/stale values make "every model report as not supported". |
| Headers on the models call | Same as `/responses`: `Authorization: Bearer …`, `chatgpt-account-id`, `originator`, `User-Agent`, `version` (+ `X-OpenAI-Fedramp` when applicable) | `codex:codex-api/src/endpoint/session.rs:56,80-110` (provider headers + `apply_auth`); `login/src/auth/default_client.rs:335-350` | 2026-09-11 | OpenAI's release workflow calls it with only `Authorization` (an API key) and `User-Agent`, so the endpoint also accepts platform API keys. Whether `chatgpt-account-id` may be omitted with a ChatGPT token is UNVERIFIED. |
| Response shape | `{"models":[ModelInfo,…]}`; per model (as serialized/deserialized by the CLI): `slug`, `display_name`, `description`, `default_reasoning_level` (`none|minimal|low|medium|high|xhigh`), `supported_reasoning_levels: [{effort, description}]`, `shell_type`, `visibility` (`list|hide|none`), `supported_in_api` (bool), `priority` (int, lower = first), `minimal_client_version` (e.g. `"0.153.0"` in bundle; array form `[0,99,0]` in a test), `additional_speed_tiers`, `service_tiers`, `default_service_tier`, `availability_nux`, `upgrade` (`{model, migration_markdown, retirement_at}`), `model_messages` (`instructions_template`, …), `base_instructions` (legacy top-level, promoted into `model_messages.instructions_template`), `supports_reasoning_summary_parameter`, `default_reasoning_summary`, `support_verbosity`, `default_verbosity`, `apply_patch_tool_type`, `web_search_tool_type`, `truncation_policy {mode, limit}`, `supports_image_detail_original`, `context_window`, `max_context_window`, `auto_compact_token_limit`, `comp_hash`, `effective_context_window_percent`, `experimental_supported_tools`, `input_modalities` (`text|image|audio`), `supports_search_tool`, `supports_experimental_context`, `use_responses_lite`, `node_repl_*`, `model_specialty`, `tool_mode`, `multi_agent_version`, `multi_agent_reasoning_effort`, `prefer_websockets`, `available_in_plans`, `supports_parallel_tool_calls` | `codex:protocol/src/openai_models.rs:398-500,766-853`; key list of the bundled catalog `codex:models-manager/models.json` | 2026-09-11 | Response header `ETag` is captured. A model lacking both `base_instructions` and `model_messages.instructions_template` fails deserialization ("model `<slug>` is missing both…"). |
| Bundled catalog snapshot (what the endpoint returned to OpenAI's workflow) | 9 models: `gpt-6-astra` (list, prio 1, min_cv 0.153.0), `gpt-5.6-sol`/`-terra`/`-luna` (list, min_cv 0.144.0), `gpt-daybreak-blue-latest`/`-red-latest` (hide), `gpt-5.5` (list, min_cv 0.124.0), `gpt-5.4` (hide, min_cv 0.98.0), `codex-auto-review` (hide); all `supported_in_api: true`, `context_window` 272000 (red: 372000) | `codex:models-manager/models.json` (generated by `rust-release-prepare.yml`) | 2026-09-11 | Actual per-account catalog varies by plan (`available_in_plans`) and `client_version`. |
| Client-side filtering | Sort by `priority`; when auth is not the Codex backend, drop models with `supported_in_api == false`; `show_in_picker = (visibility == "list")`; default = first picker-visible model. No client-side `minimal_client_version` filtering (server does it via `client_version`) | `codex:protocol/src/openai_models.rs:896,936-955`; `models-manager/src/manager.rs:150-167`; grep for `minimal_client_version` in non-test Rust = none | 2026-09-11 | `oo:models.ts` `isPublicCodexModel`: `supported_in_api !== false && (visibility undefined or "list")`. |

## F. Rate limits / errors

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| 429 body shape (usage limit) | `{"error":{"type":"usage_limit_reached","code"?:…,"plan_type"?:"<plan>","resets_at"?:<unix seconds>, "message"?:…}}` → `CodexErr::UsageLimitReached`; `error.type == "usage_not_included"` → UsageNotIncluded; `error.type == "insufficient_quota"` or `error.code ∈ {insufficient_quota, credit_balance_exhausted, organization_spend_limit_exceeded, project_spend_limit_exceeded, organization_usage_limit_exceeded}` → QuotaExceeded; any other 429 → RetryLimit | `codex:codex-api/src/api_bridge.rs:133-181,272-284` (`UsageErrorBody { code, type, plan_type, resets_at }`) | 2026-09-11 | DELTA vs brief: the CLI reads `resets_at` (absolute), not `resets_in_seconds`; `cpa:codex_executor_terminal.go:426-445` reads `resets_at` first and falls back to `resets_in_seconds`; a test fixture in `core/tests/suite/client_websockets.rs:1828` still shows `"resets_in_seconds": 1234`. Which one the server sends today is UNVERIFIED. |
| Usage-window response headers (default `codex` family) | `x-codex-primary-used-percent` (f64), `x-codex-primary-window-minutes` (i64), `x-codex-primary-reset-at` (unix seconds), `x-codex-secondary-used-percent`, `x-codex-secondary-window-minutes`, `x-codex-secondary-reset-at`, `x-codex-credits-has-credits` (`true|false|1|0`), `x-codex-credits-unlimited`, `x-codex-credits-balance` (string), `x-codex-active-limit` (limit id), `x-codex-promo-message`, `x-codex-rate-limit-reached-type` (`workspace_owner_usage_limit_reached|workspace_member_usage_limit_reached`), `x-codex-<limit>-limit-name`; additional metered families `x-<limit_id>-primary-used-percent` etc. (e.g. `x-codex-secondary-primary-used-percent`, `x-codex-bengalfox-…`) | `codex:codex-api/src/rate_limits.rs:57-102,180-231,263-272`; `codex-api/src/api_bridge.rs:212`; `protocol/src/protocol.rs:2380-2381` | 2026-09-11 | DELTA vs brief: `-reset-at`, not `-reset-after-seconds`. Header names are parsed case-insensitively. Also `x-codex-primary-over-secondary-limit-percent` appears in test fixtures (`core/tests/suite/client.rs:3349`) but is not parsed by current code. |
| Other error/diagnostic headers | `x-request-id`, `x-oai-request-id`, `cf-ray`, `x-openai-authorization-error`, `x-error-json` (base64 JSON with `error.code`) | `codex:codex-api/src/api_bridge.rs:212-217,246-270` | 2026-09-11 | |
| 401 | Treated as recoverable auth error → reload/refresh/retry (see A) | `codex:model-provider/src/provider.rs:195-200`; `core/src/client.rs:1590-1610` | 2026-09-11 | Body shape UNVERIFIED (client does not parse it). |
| 403 | Not auth-recoverable. If body contains `Cloudflare` and `blocked` → user message "Access blocked by Cloudflare. This usually happens when connecting from a restricted region (status 403 Forbidden)". 400/403 with `error.code == "misalignment_policy_violation"` → MisalignmentPolicyViolation | `codex:codex-api/src/api_bridge.rs:88-109,224-244` | 2026-09-11 | `cpa` notes Cloudflare 1010 blocks when forwarding foreign User-Agents (`codex_executor_request.go` comment on `applyCodexDirectImageHeaders`). |
| 400 | `error.code == "cyber_policy"` → CyberPolicy; body containing `The image data you provided does not represent a valid image` → InvalidImageRequest; else InvalidRequest(body) | `codex:codex-api/src/api_bridge.rs:111-130` | 2026-09-11 | |
| 503 | `error.code ∈ {server_is_overloaded, slow_down}` → ServerOverloaded | `codex:codex-api/src/api_bridge.rs:75-86` | 2026-09-11 | |
| 500 | → InternalServerError | `codex:codex-api/src/api_bridge.rs:131-132` | 2026-09-11 | |
| Retry policy | Transport retries 5xx and connection errors (max 4 attempts, 200 ms base), never 429 at transport level; `rate_limit_exceeded` inside `response.failed` carries a parsed retry delay | `codex:model-provider-info/src/lib.rs:391-397`; `codex-api/src/sse/responses.rs:458-465` | 2026-09-11 | |

## G. Local credential store

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| File | `$CODEX_HOME/auth.json` (default `~/.codex/auth.json`), mode `0600`, pretty-printed JSON | `codex:login/src/auth/storage.rs:154-156,206-223` | 2026-09-11 | |
| Top-level fields | `auth_mode` (optional; serde values `apikey`, `chatgpt`, `chatgptAuthTokens`, `headers`, `agentIdentity`, `personalAccessToken`, `bedrockApiKey`, `bedrockAccessKeys`), `OPENAI_API_KEY` (nullable string; always present), `tokens` (optional), `last_refresh` (optional RFC 3339 UTC), `agent_identity`, `personal_access_token`, `bedrock_api_key`, `bedrock_access_keys` (all optional) | `codex:login/src/auth/storage.rs:39-65`; `protocol/src/auth.rs:7-38` | 2026-09-11 | After browser login: `auth_mode: "chatgpt"`, `OPENAI_API_KEY` = exchanged key or `null`. Missing `auth_mode` resolves to `chatgpt` when `tokens` present (`manager.rs:1770-1787`). |
| `tokens` object | `{"id_token": "<raw JWT>", "access_token": "<JWT>", "refresh_token": "<string>", "account_id": "<chatgpt_account_id or null>"}` | `codex:login/src/token_data.rs:10-25,200-213` | 2026-09-11 | `id_token` is stored as the raw JWT string and re-parsed on load. Same layout written by `oo:auth-file.ts` (`auth_mode: "chatgpt"`) — the reference impls share `~/.codex/auth.json` with the CLI. |
| Example | `{"auth_mode":"chatgpt","OPENAI_API_KEY":null,"tokens":{"id_token":"eyJ…","access_token":"eyJ…","refresh_token":"rt_…","account_id":"<uuid>"},"last_refresh":"2026-09-11T03:00:00Z"}` | Composed from the structs above; test fixture `codex:model-provider/src/auth.rs:418-426` | 2026-09-11 | |
| Keychain option | `cli_auth_credentials_store = "file" | "keyring" | "auto" | "ephemeral"`; **default `file`**. Keyring backends: `direct` → OS keyring service `"Codex Auth"`, account key `cli|<first 16 hex of sha256(canonical codex_home)>`, value = the same JSON; `secrets` → encrypted local secrets store namespace `CodexAuth`, name `CODEX_AUTH`. Saving to keyring deletes `auth.json` | `codex:config/src/types.rs:106-119`; `login/src/auth/storage.rs:230-249,265-323,340-406,502-544`; `oai-auth-docs` "Credential storage" | 2026-09-11 | On macOS the `keyring` backend is the login Keychain. Admins can enforce via requirements. `ChatgptAuthTokens` mode is always in-memory (`manager.rs:1789-1798`). |
| Refresh cadence rule | see A: `exp − 5 min` or `last_refresh` older than 8 days | `codex:login/src/auth/manager.rs:191-192,2959-2981` | 2026-09-11 | |

## H. Policy

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Official Codex auth docs | Describe only first-party clients (ChatGPT desktop app, Codex CLI, IDE extension, Codex cloud); "Codex caches login details locally in a plaintext file at `~/.codex/auth.json` or in your OS-specific credential store"; "For sign in with ChatGPT sessions, Codex refreshes tokens automatically"; enterprise "Codex access tokens" exist for non-interactive workflows (`codex login --with-access-token`). No statement on third-party clients | `oai-auth-docs` (https://developers.openai.com/codex/auth) | 2026-09-11 | |
| Terms that apply | "When you sign in to Codex using an existing ChatGPT account, the ChatGPT Terms of Use and Privacy Policy … apply to data shared between Codex and ChatGPT." | `oai-help` (https://help.openai.com/en/articles/11369540-codex-in-chatgpt-faq, updated 2026-09-11) | 2026-09-11 | No mention of third-party clients or the OAuth client id. |
| CEO statement on third-party use | Sam Altman, X post 2026-05-01/02: "you can sign in to openclaw with your chatgpt account now and use your subscription there! happy lobstering." (OpenClaw is a third-party agent framework using this same Codex OAuth) | Quoted verbatim by The Next Web 2026-05-03, Storyboard18 2026-05-04, News9live 2026-05-04 (`news`). The post URL https://x.com/sama/status/2050357911915028689 returned HTTP 403 to our fetch | 2026-09-11 | Not an OpenAI policy document. The Verge 2026-05-15 cites an OpenAI blog post by Nik Pash ("your ChatGPT subscription can now power an OpenClaw agent"); we could not locate that post on openai.com via search — UNVERIFIED URL. |
| Third-party doc claiming sanction | "OpenAI Codex OAuth is explicitly supported for use outside the Codex CLI, including OpenClaw workflows." / "OpenAI explicitly supports subscription OAuth usage in external tools and workflows like OpenClaw." Documented flow: PKCE, `https://auth.openai.com/oauth/authorize?...` (scope `openid profile email offline_access`), callback `http://localhost:1455/auth/callback`, exchange at `https://auth.openai.com/oauth/token`, store `{access, refresh, expires, accountId}` | `openclaw-docs` (https://docs.openclaw.ai/concepts/oauth, https://docs.openclaw.ai/providers/openai, live 2026-09-11) | 2026-09-11 | OpenClaw's founder is an OpenAI employee (The Verge 2026-05-15), but the docs are not an OpenAI publication. |
| Contrary/secondary views | developer.puter.com (2026-06-11): "none of them are designed to let a third-party app call the OpenAI API on a user's behalf … Not officially supported"; gist ravidsrk (2026-06-20) calls it "a ToS-violating cost hack"; community thread 1371778 (2026-01-16) asks whether third parties should reuse `app_EMoa…`; thread 1389585 (2026-08-08) requests an independent client registration — no OpenAI answer visible | `news` | 2026-09-11 | Opinions, not policy. |
| "Sign in with ChatGPT" (Aug 2026) | Launched in beta 2026-08-02 as an identity provider for partner sites (Airtable, GitLab, HubSpot, Notion, Supabase, Vercel); partners receive name/email/picture only — it is NOT model access on the user's plan | TechTimes 2026-08-03, dreaming.press 2026-08-03 (`news`) | 2026-09-11 | Distinct from the Codex OAuth client. A "ChatPass"/subscription-sharing meter was found dormant in Codex desktop builds (RuntimeWire/Superpower Daily 2026-08-27) — no launch. |
| Reference-impl legal posture | `oo:README.md` "Legal": unofficial; "Each person must use their own ChatGPT account … Do not pool, share, or redistribute access tokens … OpenAI may change or disable the underlying services at any time." | `oo:README.md:599-609` | 2026-09-11 | |
| Bottom line | No OpenAI-authored document found (as of 2026-09-11) that either authorizes or prohibits third-party desktop apps using the Codex OAuth client id and `chatgpt.com/backend-api/codex`. Strongest official-adjacent signals are permissive (CEO post; OpenAI-employee-run OpenClaw docs); official docs are silent. | all of the above | 2026-09-11 | |

## Items still UNVERIFIED (and how a Mac capture would settle them)

Capture plan: run the stock `codex` CLI (`rust-v0.154.0`) on the Mac with `HTTPS_PROXY` pointed at mitmproxy (the CLI honours system/PAC proxies and has a custom-CA fallback path, `default_client.rs:305-310`), do `codex login`, one chat turn with an image, and `/status`; then replay variants with `curl`.

1. **Server-side requirement for the Codex system prompt in `instructions`.** Reference impls send `""` or a custom sentence and claim success; a June 2026 article claims validation. Settle: replay the captured `/responses` request with `instructions` replaced by `""` and by a custom sentence; record status/body.
2. **Whether `OpenAI-Beta: responses=experimental` is ignored, required, or rejected.** The CLI no longer sends it on HTTP. Settle: replay with and without the header.
3. **Whether the 4-scope string (without `api.connectors.*`) still yields a token accepted by the Codex backend.** Settle: run the loopback flow with the shorter scope and call `/models` and `/responses`.
4. **Whether `chatgpt-account-id` may be omitted** with a ChatGPT bearer token (OpenAI's workflow omits it with an API key). Settle: replay `/models` and `/responses` without the header.
5. **Which reset field the 429 body carries today** (`resets_at` vs `resets_in_seconds`) and whether `x-codex-*-reset-after-seconds` headers still exist. Settle: exhaust a Free/Plus window or inspect any 429 capture's headers/body.
6. **Access-token lifetime (`exp`), presence of `expires_in`/`token_type` in the token response, and presence of `name`/`organizations`/`chatgpt_subscription_*` claims.** Settle: decode the captured token response and JWT payloads.
7. **Device-code `interval` actual value and response type (string vs int).** Settle: capture `POST /api/accounts/deviceauth/usercode`.
8. **Whether the refresh endpoint accepts form-encoding** (CLIProxyAPI uses it) in addition to the CLI's JSON. Settle: replay both.
9. **401/403 response body shapes from `chatgpt.com/backend-api/codex`.** The client does not parse them. Settle: replay `/responses` with an expired/garbled bearer and with a mismatched `chatgpt-account-id`.
10. **Exact image size/pixel limits** (live in `codex-utils-image`, not read). Settle: read that crate or send progressively larger data URLs.
11. **npm `@openai/codex` latest version** (registry blocked in sandbox; GitHub says `rust-v0.154.0`). Settle: `npm view @openai/codex version` on the Mac.
12. **The Nik Pash / OpenAI blog post URL cited by The Verge (2026-05-15)** and the exact text of Sam Altman's X post (X returned 403). Settle: open in a browser on the Mac.
13. **Whether `x-oai-attestation` is ever required.** The CLI sends it only when a host `AttestationProvider` is wired in (`client.rs:454,732-740`); no evidence of a hard requirement. Settle: observe whether stock CLI on macOS sends it.

## Deltas vs the brief

| Brief says | Verified today | Source |
|---|---|---|
| scopes `openid profile email offline_access` | CLI sends `openid profile email offline_access api.connectors.read api.connectors.invoke`; all three reference impls still use the brief's 4 scopes | `codex:login/src/server.rs:590-591`; `oo`, `lwc`, `cpa` constants |
| `OpenAI-Beta: responses=experimental` on requests | The current CLI sends **no** `OpenAI-Beta` header on HTTP `/responses`; only WebSocket uses `OpenAI-Beta: responses_websockets=2026-02-06`. `lwc` still sends `responses=experimental`; `oo` strips the header | `codex:core/src/client.rs:166,1175-1178`; repo grep |
| redirect `http://localhost:1455/auth/callback` (fixed) | Default 1455 with a registered fallback **1457**; path fixed | `codex:login/src/server.rs:60-62` |
| device flow at `https://auth.openai.com/codex/device` | That is the user verification page. API: `POST https://auth.openai.com/api/accounts/deviceauth/usercode` then poll `POST …/deviceauth/token`; returns `authorization_code` + server PKCE pair, exchanged at `/oauth/token` with `redirect_uri=https://auth.openai.com/deviceauth/callback` | `codex:login/src/device_code_auth.rs` |
| token endpoint `https://auth.openai.com/oauth/token` | Confirmed; code exchange is form-encoded, refresh is JSON (`{client_id, grant_type:"refresh_token", refresh_token}`) | `codex:login/src/server.rs:836`; `manager.rs:1613-1614` |
| headers `session_id`, `conversation_id` (implied) | `session-id`, `thread-id`, `x-client-request-id` | `codex:codex-api/src/requests/headers.rs` |
| `originator: codex_cli_rs` | Confirmed as default; TUI/desktop/exec variants differ (`codex-tui`, `codex_exec`, `codex_chatgpt_desktop`) | `codex:login/src/auth/default_client.rs:40,153-162` |
| 429 `resets_in_seconds` | CLI parses `resets_at` (unix seconds); `resets_in_seconds` only in an old test fixture and CLIProxyAPI fallback | `codex:codex-api/src/api_bridge.rs:283` |
| headers `x-codex-primary-reset-after-seconds` etc. | Current names: `x-codex-primary-reset-at`, `x-codex-secondary-reset-at` (plus `-used-percent`, `-window-minutes`, credits headers, `x-codex-active-limit`, `x-codex-rate-limit-reached-type`, `x-codex-promo-message`) | `codex:codex-api/src/rate_limits.rs:68-102` |
| catalog `GET /backend-api/codex/models?client_version=` | Confirmed; current stable value `0.154.0`; response is `{"models":[…]}` keyed by `slug` (not `id`) | `codex:codex-api/src/endpoint/models.rs`; `codex-rel` |
| plan type + account id in the ID token under `https://api.openai.com/auth` | Confirmed (`chatgpt_plan_type`, `chatgpt_account_id`); the same namespace is also present in the access token and the CLI reads plan type from the access token for its success page | `codex:login/src/token_data.rs`; `success_page.rs:77-81` |
| `~/.codex/auth.json` | Confirmed layout; default store mode is `file`; `keyring`/`auto` put the same JSON in the macOS Keychain under service `Codex Auth` | `codex:login/src/auth/storage.rs`; `config/src/types.rs:109-119` |
| body fields incl. `previous_response_id` | `previous_response_id` is never sent over HTTP (`store: false`, full history replayed in `input`); `include` is exactly `["reasoning.encrypted_content"]`; `prompt_cache_key` = session id | `codex:codex-api/src/common.rs:259-310`; `core/src/client.rs:852-889` |
