# Image tokens, resolution hints, caching, WebP — verified 2026-09-11

Scope: facts an implementer needs for (a) provider resolution hints that cut image prefill tokens, (b) provider-side prompt caching, (c) JPEG-vs-WebP for the frame encoder on macOS 14+. Every value below was read from the cited page on 2026-09-11 (direct HTTPS GET with `curl -k` and/or Exa livecrawl; Apple docs read through Apple's own `/tutorials/data/...json` data endpoints because the HTML pages are JS shells). Anything the pages did not state is marked **UNVERIFIED**. Nothing was filled from memory. Where a number is *computed* from a documented formula it is labelled "derived".

Method note: ai.google.dev now renders every guide in two views — the default "Interactions API" view (`/gemini-api/docs/<page>`) and the "generateContent API (Legacy)" view (`/gemini-api/docs/generate-content/<page>`). Both were read; field names differ (`resolution: "high"` vs `mediaResolution: {level: "MEDIA_RESOLUTION_HIGH"}`).

## Sources consulted (url, fetched date, used for)

| # | URL | Fetched | Used for |
|---|---|---|---|
| S1 | https://raw.githubusercontent.com/bloxy-studios/bluey/main/docs/reference/gemini-api-sept-2026.md | 2026-09-11 (HTTP 200, 82 KB; doc is self-dated 2026-09-08) | Project reference doc — cross-check (see end of section C) |
| S2 | https://ai.google.dev/gemini-api/docs/image-understanding and https://ai.google.dev/gemini-api/docs/generate-content/image-understanding | 2026-09-11 | A: MIME types, 20 MB inline limit, 3,600 images, tiling (258 / 768×768), media_resolution pointer |
| S3 | https://ai.google.dev/gemini-api/docs/media-resolution and https://ai.google.dev/gemini-api/docs/generate-content/media-resolution | 2026-09-11 | A: enum names, per-part vs global placement, token table per level |
| S4 | https://ai.google.dev/api/generate-content | 2026-09-11 | A/B/C: `Part.mediaResolution`, `MediaResolution`/`Level` enums, `GenerationConfig.mediaResolution`, `ThinkingConfig`/`ThinkingLevel`, `UsageMetadata`, `Blob` |
| S5 | https://ai.google.dev/gemini-api/docs/thinking and https://ai.google.dev/gemini-api/docs/generate-content/thinking | 2026-09-11 | B: thinking-level support table per model, latency guidance |
| S6 | https://ai.google.dev/gemini-api/docs/whats-new-gemini-3.5 | 2026-09-11 | B: default effort, level descriptions, media_resolution migration notes |
| S7 | https://ai.google.dev/gemini-api/docs/caching and https://ai.google.dev/gemini-api/docs/generate-content/caching | 2026-09-11 | C: implicit-caching minimums, explicit caching, TTL, usage fields |
| S8 | https://ai.google.dev/api/caching | 2026-09-11 | C: `cachedContents` REST resource (ttl/expireTime, immutability) |
| S9 | https://ai.google.dev/gemini-api/docs/pricing | 2026-09-11 | C: cache read/storage prices for gemini-3.8-flash and gemini-3.5-flash-lite |
| S10 | https://ai.google.dev/gemini-api/docs/models, https://ai.google.dev/gemini-api/docs/models/gemini-3.8-flash, https://ai.google.dev/gemini-api/docs/models/gemini-3.5-flash-lite | 2026-09-11 | A/B/C: model IDs, capability rows (Caching, Thinking) |
| S11 | https://ai.google.dev/gemini-api/docs/files, https://ai.google.dev/gemini-api/docs/generate-content/files, https://ai.google.dev/gemini-api/docs/file-input-methods | 2026-09-11 | A: Files API thresholds (100 MB / 2 GB / 20 GB / 48 h) |
| S12 | https://ai.google.dev/gemini-api/docs/tokens | 2026-09-11 | A: image token rule restated; Interactions `usage.*` field names |
| S13 | https://ai.google.dev/api/models | 2026-09-11 | G: `models.get` / `models.list` endpoints |
| S14 | https://ai.google.dev/gemini-api/docs/api-key | 2026-09-11 | G: `x-goog-api-key` header; key-type migration |
| S15 | https://platform.openai.com/docs/guides/images-vision (identical body served at https://developers.openai.com/api/docs/guides/images-vision) | 2026-09-11 | D: `detail` values, model sizing table, patch/tile token rules, limits |
| S16 | https://platform.openai.com/docs/guides/prompt-caching | 2026-09-11 | D: 1,024-token minimum, `cached_tokens`, `prompt_cache_key`, TTL/retention |
| S17 | https://developers.openai.com/api/reference/resources/responses/methods/create | 2026-09-11 | D: `ResponseInputImage` schema, `prompt_cache_key`, `prompt_cache_options` |
| S18 | https://developers.openai.com/api/reference/resources/models/methods/list | 2026-09-11 | G: `GET /v1/models` |
| S19 | https://platform.claude.com/docs/en/build-with-claude/vision (also read as `.md`; https://docs.anthropic.com/en/docs/build-with-claude/vision 301-redirects here) | 2026-09-11 | E: resolution tiers, 28-px patch formula, formats, size limits |
| S20 | https://platform.claude.com/docs/en/build-with-claude/prompt-caching (also read as `.md`) | 2026-09-11 | E: minimum cacheable tokens per model, TTLs, 4 breakpoints, system blocks, usage fields |
| S21 | https://developer.apple.com/documentation/uniformtypeidentifiers/uttype-swift.struct/webp and .../uttypewebp (via `/tutorials/data/documentation/...json`) | 2026-09-11 | F: `UTType.webP` identifier and OS availability |
| S22 | https://developer.apple.com/documentation/imageio/cgimagedestinationcopytypeidentifiers() and .../cgimagesourcecopytypeidentifiers() and .../cgimagedestination (JSON data endpoints) | 2026-09-11 | F: the documented way to enumerate encodable/decodable UTIs |
| S23 | https://developer.apple.com/videos/play/wwdc2023/10122/ (transcript) | 2026-09-11 | F: WWDC23 statement on WebP (Safari/Big Sur) |
| S24 | Third-party, anecdotal (flagged as such): https://stackoverflow.com/questions/68802152 (2021-08-16); https://github.com/SDWebImage/SDWebImage/pull/3048 (2020); https://zenn.dev/noppe/articles/59a05076f23991 (published 2024-11-20); https://github.com/fluttercandies/flutter_image_compress/issues/369 (2026-07-15) | 2026-09-11 | F: evidence for/against ImageIO WebP encoding |
| — | https://developer.apple.com/forums/thread/732236 and https://developer.apple.com/forums/search?q=CGImageDestination+webp | 2026-09-11 | F: curl hit a bot-verification wall; a real browser session showed the search page with zero results. No Apple-forum datapoint obtained. |
| — | https://platform.openai.com/docs/api-reference/* | 2026-09-11 | Returned HTTP 403 to curl and failed Exa crawl; the same reference is served openly at developers.openai.com (S17, S18). |

## A. Gemini image tokens & resolution hints

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Model IDs the docs list today | `gemini-3.8-flash` ("New Stable", latest update September 2026) and `gemini-3.5-flash-lite` (Stable, latest update July 2026) both appear in the "All Gemini 3 models" endpoint table, alongside `gemini-3.7-flash`, `gemini-3.6-flash`, `gemini-3.5-flash`, `gemini-3.1-flash-lite`, `gemini-3.1-pro-preview`, `gemini-3-flash-preview`. | S10 | 2026-09-11 | Both model pages: inputs "Text, Image, Video, Audio, and PDF"; 1,048,576 in / 65,536 out. |
| Where `mediaResolution` lives (generateContent, per part) | On the **Part**, as a sibling of `inline_data` — NOT inside `inlineData`. REST sample: `{"inline_data": {"mime_type": "image/jpeg", "data": "..."}, "media_resolution": {"level": "MEDIA_RESOLUTION_HIGH"}}`. API ref: `Part.mediaResolution` — "object (MediaResolution). Optional. Media resolution for the input media." `MediaResolution` has one field `level` (enum `Level`). | S3 (legacy view), S4 | 2026-09-11 | Guide: "The media_resolution parameter for parts is available in the v1beta API version." and "Note: Per-part media resolution is an experimental feature." Per-part is "Gemini 3 only". |
| Where `mediaResolution` lives (generateContent, global) | `generationConfig.mediaResolution` — a bare enum string: `"generation_config": {"media_resolution": "MEDIA_RESOLUTION_HIGH"}`. "This is supported by all multimodal models. If a request includes both global and per-part settings, the per-part setting takes precedence for that specific item." | S3, S4 | 2026-09-11 | API ref field text: "Optional. If specified, the media resolution specified will be used." |
| Enum names (per-part `Level`) | `MEDIA_RESOLUTION_UNSPECIFIED`, `MEDIA_RESOLUTION_LOW`, `MEDIA_RESOLUTION_MEDIUM`, `MEDIA_RESOLUTION_HIGH`, `MEDIA_RESOLUTION_ULTRA_HIGH` | S4 (`## Level`) | 2026-09-11 | Guide: "MEDIA_RESOLUTION_ULTRA_HIGH (Per part only)". |
| Enum names (GenerationConfig `MediaResolution`) | `MEDIA_RESOLUTION_UNSPECIFIED`, `MEDIA_RESOLUTION_LOW`, `MEDIA_RESOLUTION_MEDIUM`, `MEDIA_RESOLUTION_HIGH` (no ULTRA_HIGH) | S4 | 2026-09-11 | **Trap:** the API-reference doc strings for this enum still say "low (64 tokens)", "medium (256 tokens)", "high (zoomed reframing with 256 tokens)". Those are the Gemini 2.5 numbers (see the 2.5 table below), not Gemini 3. The guide explicitly says the unspecified level's "token count ... varies significantly between Gemini 3 and earlier Gemini models." Trust the guide table, not the enum comments. |
| Interactions API equivalent | Per content item: `{"type": "image", "data"/"uri": ..., "mime_type": "image/jpeg", "resolution": "high"}`; values `unspecified`, `low`, `medium`, `high`, `ultra_high` ("Per content item only"). | S3 (Interactions view) | 2026-09-11 | The Interactions view of the guide documents only per-item control ("You can configure media resolution for individual media objects (content items) within your request (Gemini 3 only)"). |
| Token cost per level, images, **"Gemini 3 models"** table | `unspecified (Default)` **1120**; `low` **280**; `medium` **560**; `high` **1120**; `ultra_high` **2240** | S3 | 2026-09-11 | Same table gives video (per frame): 70 / 70 / 70 / 280 / N/A and PDF: 560 / 280+Native Text / 560+Native Text / 1120+Native Text / N/A. Header wording: "approximate token counts for each media_resolution value and media type per model family." |
| Default level when omitted | `unspecified`, which costs the same as `high` (1120 tokens/image) on Gemini 3 models. "Note that high provides the optimal performance for most use cases." Recommended setting for Images: `high` (Max Tokens 1120). | S3 | 2026-09-11 | So omitting the field does NOT give you MEDIUM. |
| Per-model numbers for `gemini-3.8-flash` / `gemini-3.5-flash-lite` | **UNVERIFIED** — the table is per "Gemini 3 models" family only; neither model page (S10) nor any other page read states per-model image token counts. | S3, S10 | 2026-09-11 | Settle by calling `countTokens` (or reading `usageMetadata.promptTokensDetails[]` modality counts) with one screenshot per level on each model. |
| Gemini 2.5 table (for contrast) | `UNSPECIFIED (Default)` 256 + Pan & Scan (~2048); `LOW` 64; `MEDIUM` 256; `HIGH` 256 + Pan & Scan (images) | S3 (legacy view) | 2026-09-11 | Explains the 64/256 numbers in the API-reference enum comments. |
| Tiling rule | "258 tokens if both dimensions <= 384 pixels. Larger images are tiled into 768x768 pixel tiles, each costing 258 tokens." Rough tile count: crop unit ≈ `floor(min(width, height) / 1.5)`; tiles = (width/crop) × (height/crop); example 960×540 → crop 360 → 3 × 2 = 6 tiles. | S2 (both views), S12 | 2026-09-11 | This section is **not model-qualified** and sits directly above the media_resolution section; the docs never reconcile "258 per 768-px tile" with "1120 per image" for Gemini 3. Treat the media_resolution table as authoritative for Gemini 3 and verify with `countTokens`. |
| Max tiles per image | **UNVERIFIED** — not stated on S2, S3 or S12. | — | 2026-09-11 | |
| Supported image MIME types | `image/png`, `image/jpeg`, `image/webp`, `image/heic`, `image/heif` | S2 | 2026-09-11 | API-ref `Blob.mimeType` examples additionally list `image/jpg`, `image/gif`, `image/avif` (S4). |
| Inline size limit | "Inline image data limits your total request size (text prompts, system instructions, and inline bytes) to 20MB. For larger requests, upload image files using the File API." | S2 (both views) | 2026-09-11 | **Conflict:** the File input methods page (S11) says "Inline data: 100 MB per request or payload (50 MB for PDFs)" and the Files API guide says "Always use the Files API when the total request size ... is larger than 100 MB. For PDF files, the limit is 50 MB." Design to the stricter 20 MB. |
| Files API thresholds | 2 GB per file; 20 GB per project; files stored 48 hours; free of charge. | S11 | 2026-09-11 | Upload is a resumable `POST {BASE_URL}/upload/v1beta/files` with `x-goog-api-key`. |
| Max images per request | 3,600 | S2 | 2026-09-11 | |
| Prompt ordering tip | "When using a single image with text, place the text prompt before the image in the input array." (Interactions view) vs legacy view "place text prompts after the image part". Files guide: "Put your image first for single-image prompts". | S2, S11 | 2026-09-11 | Contradictory across pages; low stakes. |

## B. Gemini thinking

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Field names | generateContent: `generationConfig.thinkingConfig.thinkingLevel` (enum `ThinkingLevel`: `THINKING_LEVEL_UNSPECIFIED`, `MINIMAL`, `LOW`, `MEDIUM`, `HIGH`; guides pass lower-case strings `"low"` etc.). Interactions: `generation_config.thinking_level`. | S4, S5 | 2026-09-11 | API ref: "The default value is model-dependent." `thinkingBudget` still exists; "Important: You cannot use both thinking_level and the legacy thinking_budget parameter in the same request. Doing so will return a 400 error." (S6) |
| Levels accepted by `gemini-3.8-flash` (and 3.7) | `low`, `medium` (Default), `high` (Dynamic). **`minimal`: "Not supported (error)"**. Model page: "Thinking: Supported (low, medium, high). Note: minimal is not supported and returns an error." | S5 (legacy table column "Gemini 3.8 & 3.7 Flash"), S5 Interactions table ("gemini-3.8-flash | On (medium) | low, medium, high"), S10 | 2026-09-11 | So `minimal` is NOT accepted on the current Flash tier. |
| Levels accepted by `gemini-3.5-flash-lite` (and 3.1 Flash-Lite) | `minimal` (Default), `low`, `medium`, `high` | S5 (column "Gemini 3.5 & 3.1 Flash-Lite"; Interactions table "gemini-3.5-flash-lite | On (minimal) | minimal, low, medium, high") | 2026-09-11 | |
| Other 3.x rows | 3.6 & 3.5 Flash: minimal/low/medium(Default)/high. 3.1 Pro: no minimal, high default. 3-flash-preview: all four, high default. | S5 | 2026-09-11 | |
| Can thinking be turned off? | No. "You cannot disable thinking for Gemini 3.1 Pro. Gemini 3 Flash and Flash-Lite also do not support full thinking-off." `minimal` "does not guarantee that thinking is off". | S5 | 2026-09-11 | |
| Latency / TTFT guidance | `high`: "The model may take significantly longer to reach a first (non thinking) output token". `low`: "Minimizes latency and cost." `minimal`: "Optimized for response speed. Chat-like use cases, quick factual answers, simpler tool calls." "To reduce cost or latency without truncating responses, lower thinking_level (low or medium) instead of setting a small max_output_tokens." `max_output_tokens` is a hard cutoff that includes thought tokens and bills them even when truncated. | S5, S6 | 2026-09-11 | No numeric TTFT/latency figures are published → numbers **UNVERIFIED**. |
| Default effort history | "The default thinking effort is now medium, changed from high in Gemini 3 Flash Preview." | S6 | 2026-09-11 | |
| Sampling params | Migration checklist: "Remove temperature, top_p, top_k from your config (no longer recommended)." | S6 | 2026-09-11 | Bonus; relevant to cache-prefix stability and to avoiding surprises. |

## C. Gemini caching

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Implicit caching default | "Implicit caching is enabled by default for all Gemini 2.5 and newer models." Interactions: "supported for both stateful (using previous_interaction_id) and stateless conversation modes." Legacy page labels it "no cost saving guarantee" (explicit = "cost saving guarantee"). | S7 | 2026-09-11 | |
| Implicit minimum input tokens | Gemini 3.8 Flash **4,096**; 3.7 Flash 4,096; 3.6 Flash 4,096; 3.5 Flash 4,096; 3.1 Pro Preview 4,096; 2.5 Flash 2,048; 2.5 Pro 2,048 | S7 (identical table in both views) | 2026-09-11 | The 1,024 figure from older docs is gone. |
| Implicit minimum for `gemini-3.5-flash-lite` | **UNVERIFIED** — Flash-Lite is absent from the table. The model page says "Caching: Supported" and pricing lists a context-caching price, so caching exists; the threshold is simply not published. | S7, S9, S10 | 2026-09-11 | Settle empirically: send the same ≥4,096-token prefix twice and read `usageMetadata.cachedContentTokenCount`. |
| Hit-count reporting | generateContent: `usageMetadata.cachedContentTokenCount` — "Number of tokens in the cached part of the prompt (the cached content)"; `promptTokenCount` "includes the number of tokens in the cached content"; `cacheTokensDetails[]` gives per-modality cached counts. Interactions: `usage.total_cached_tokens`. | S4, S7, S12 | 2026-09-11 | |
| How to raise hit rate | "Try putting large and common contents at the beginning of your prompt"; "Try to send requests with similar prefix in a short amount of time". | S7 | 2026-09-11 | For a screenshot copilot the per-request image should come after the static system/instructions. |
| Explicit caching availability | generateContent only: "The Interactions API only supports implicit caching. Explicit caching ... is not supported in the Interactions API." | S7 | 2026-09-11 | |
| Explicit caching mechanics | `POST https://generativelanguage.googleapis.com/v1beta/cachedContents` with `model` (required, immutable, `models/{model}`), `contents[]`, `systemInstruction`, `tools`, `toolConfig`, and `ttl` (Duration string, e.g. `"300s"`) or `expireTime` (RFC 3339). Use in generation via `cachedContent: "cachedContents/{id}"`. Only expiration is updatable (`PATCH ... updateMask`). | S7, S8 | 2026-09-11 | |
| Explicit default TTL | "If not set, the TTL defaults to 1 hour." "There are no minimum or maximum bounds on the TTL." | S7 | 2026-09-11 | |
| Explicit minimum token count | **UNVERIFIED** as a separate number — the page says "The minimum input token count for context caching varies by model" and offers only the implicit-caching table above. | S7 | 2026-09-11 | Assume the same table until tested. |
| Pricing units (gemini-3.8-flash, paid, standard) | Input $0.75/1M through 2026-12-31 ($1.50 from 2027-01-01); output $3.75 ($7.50); **context caching $0.075/1M tokens ($0.15 from 2027) plus storage $0.50 per 1,000,000 tokens per hour ($1.00 from 2027)**. Free tier: caching "Free of charge". | S9 | 2026-09-11 | Batch/Flex halve the token prices; storage price unchanged. |
| Pricing units (gemini-3.5-flash-lite, paid, standard) | Input $0.30 (text/image/video/audio); output $2.50; **context caching $0.03/1M tokens plus $1.00 per 1,000,000 tokens per hour storage**. Free tier: caching "Not available". | S9 | 2026-09-11 | |
| Cross-check vs project reference doc (S1) | S1 says: per-item `resolution: "high"` = 1,120 tokens/image; inline ≤ 20 MB per image guide vs 100 MB on file-input page; implicit caching 4,096 for 3.8/3.7/3.6/3.5 Flash & 3.1 Pro (2,048 for 2.5); hits in `usageMetadata.cachedContentTokenCount` / `usage.total_cached_tokens`; `minimal` errors on 3.7/3.8, default `medium` on 3.5+ Flash, `minimal` default on 3.5-flash-lite; "the generateContent API-reference enum text still describes 2.x token counts (64/256) — trust the media-resolution guide table". | S1 | 2026-09-11 | All of these match what the live pages said today; no drift found in three days. S1 does not state the Flash-Lite caching threshold either. |

## D. OpenAI Responses API images & caching

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| `input_image` part fields | `type: "input_image"` ("Always input_image"); `image_url: optional string or null` — "A fully qualified URL or base64 encoded image in a data URL."; `file_id: optional string or null`; `detail` — "One of high, low, auto, or original. Defaults to auto."; optional `prompt_cache_breakpoint: {"mode": "explicit"}`. | S17 | 2026-09-11 | Data-URL form used in the guide: `"image_url": "data:image/jpeg;base64,..."`. |
| Image input requirements | File types PNG, JPEG, WEBP, non-animated GIF; "Up to 512 MB total payload per request"; "Up to 1,500 images per request"; up to **30,000 patches per image** after resizing — images over that are **rejected, not resized**. | S15 | 2026-09-11 | |
| How current models tokenize images | **Patch-based**, 32 × 32 px patches: `patch_count = ceil(width/32) × ceil(height/32)`; if the model/detail has a patch budget, shrink by `sqrt((32^2 × patch_budget) / (width × height))` (with the documented floor adjustment), recount, then **multiply by the model multiplier and round up**. Multiplier **1.2** for `gpt-6-astra`, `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.4-nano`, `gpt-5.2`, `gpt-5-mini*`; 1.5 `gpt-5-nano*`; 1.62 `gpt-4.1-mini`. | S15 | 2026-09-11 | Documented examples (gpt-6-astra, detail high): 1024×1024 → 1024 patches → 1229 tokens; 2048×2048 → budget-shrunk to 1600×1600 → 2500 patches → 3000 tokens; 4096×512 → 2048 patches → 2458 tokens. |
| Model sizing behaviour (current) | `gpt-6-astra`: low fits within 512×512; high ≤ 2,500 patches and 65,535-px max dimension; original preserves dimensions (only scales >65,535 px), rejects >30,000 patches; **auto = original**. `gpt-5.6-sol/terra/luna`: low 512×512; high within 2048×2048 and 2,500 patches; original as above; **auto = original**. `gpt-5.5`: low 512×512; high 2,500 patches & 2048 px; original 10,000 patches & 6000 px; auto = original. `gpt-5.4(-mini/-nano)`: low = 2048 px & 6,144-patch budget ("so it can use more tokens than high"); high 2,500 patches & 2048 px; original 10,000 patches & 6000 px; **auto = high**. `gpt-5.2`, `gpt-4.1-mini`: low/high/auto all 2048 px & 6,144 patches; no original. | S15 | 2026-09-11 | Consequence for a screenshot copilot on gpt-6-astra / gpt-5.6-*: leaving `detail` at the default (`auto` = `original`) sends the full-resolution frame — e.g. a 2560×1600 Retina capture is 80×50 = 4000 patches × 1.2 = **4800 tokens (derived)**, vs ≤ 3000 at `high` and roughly 192 at `low` (512×320 → 160 patches × 1.2, derived). Set `detail` explicitly. |
| Token cost of `detail: "low"` on current models | **Not a fixed number.** Guide: "Resizing and token use depend on the model; low does not always use fewer tokens than high." For gpt-6-astra / gpt-5.6-* / gpt-5.5, low = fit within 512×512 then patch formula × 1.2 → ≤ 256 patches → ≤ 308 tokens (derived). | S15 | 2026-09-11 | The fixed 85-token figure belongs to the tile-based legacy models below. |
| Tile-based legacy formula (older models only) | Base + tiles: `gpt-5.1` 70 base / 140 per tile; `gpt-5*` 70/140; `gpt-4o`, `gpt-4.1` **85/170**; `gpt-4o-mini` 2833/5667; `o1*`, `o1-pro*`, `o3*` 75/150. `low` = base tokens only. `high`/`auto`: fit within 2048×2048, then shortest side to 768 px, count **512-px tiles** × tile tokens + base. | S15 | 2026-09-11 | `*` = deprecated and scheduled for shutdown. |
| Codex model image costs | **UNVERIFIED** — no `*-codex*` model appears in the vision sizing or multiplier tables (S15); the models page (S19) shows GPT-5.6 / GPT-6 families. Codex names appear only in the caching guide's extended-retention list (`gpt-5.1-codex-max`, `gpt-5.1-codex`, `gpt-5.1-codex-mini`, `gpt-5-codex`). | S15, S16, S19 | 2026-09-11 | |
| Prompt caching default & minimum | "Prompt caching is enabled by default for supported OpenAI models." "The minimum cacheable prompt length is 1,024 tokens for GPT-5.6 and later and varies by request settings for earlier models." Hidden OpenAI system tokens don't count toward the minimum. | S16 | 2026-09-11 | For pre-5.6 models the minimum "varies with request settings, including tools, images, output schemas, reasoning effort, and verbosity." |
| Cache pricing (GPT-5.6+) | Writes 1.25× uncached input rate; reads 0.1×. | S16 | 2026-09-11 | |
| Hit reporting | `usage.input_tokens_details.cached_tokens` (sample also shows `cache_write_tokens`). GPT-5.6+: "Exact eligible boundary, excluding hidden tokens"; earlier models: "rounds down to a multiple of 128". | S16 | 2026-09-11 | |
| `prompt_cache_key` | Optional string. Reference: "Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the user field." Guide: "Use prompt_cache_key when you want to maintain separate cache accounting for customers, users, or workspaces ... The key is optional and is not needed to optimize caching." | S16, S17 | 2026-09-11 | |
| Cache lifetime controls | GPT-5.6+: `prompt_cache_options.ttl` — "The only supported value, 30m, is also the default"; `prompt_cache_options.mode` = `implicit` (default) or `explicit`; up to **four** cache writes/breakpoints per request via `prompt_cache_breakpoint`. Earlier models: `prompt_cache_retention` = `in_memory` (≈5–10 min inactivity, up to 1 h) or `24h`; "For gpt-5.5, gpt-5.5-pro, and future models, only 24h is supported." | S16, S17 | 2026-09-11 | Settings that change the cached prefix: `model`, `tools`, `parallel_tool_calls`, `text.format`, `reasoning.effort`, `text.verbosity`, `context_management`. |
| Images are cacheable | "OpenAI caches the model's full rendered context including ... conversation history containing text, images, documents, and supported audio." | S16 | 2026-09-11 | A per-request screenshot placed last keeps the static prefix cacheable. |

## E. Anthropic images & caching

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Token formula | "Claude views images in patches instead of pixels. Each patch is a 28×28-pixel block of the image, referred to as a visual token. An image, therefore, costs ⌈width / 28⌉ × ⌈height / 28⌉ visual tokens." | S19 | 2026-09-11 | The `width×height/750` formula and the "~1.15 megapixel" phrasing no longer appear on the page (0 grep hits). |
| Resolution tiers | **High-resolution** (Claude 4.7 and later models): max long edge **2576 px**, max **4784** visual tokens. **Standard** (all other models): max long edge **1568 px**, max **1568** visual tokens. "Images larger than either limit are downscaled before processing." "High-resolution support is automatic on the listed models and requires no beta header or client-side opt-in." | S19 | 2026-09-11 | Worked rows: 1000×1000 → 1296 tokens (both tiers); 1092×1092 (1.19 MP) not resized → 1521; 1920×1080 → standard: downsized to 1456×819 = 1560 tokens, high-res: not resized = 2691; 3840×2160 → standard 1456×819 = 1560, high-res 2576×1449 = 4784. "High-resolution images can use up to roughly three times more visual tokens ... downsample images before sending to control token costs." |
| Supported types | `image/jpeg`, `image/png`, `image/gif`, `image/webp`; "Animations are unsupported, and only the first frame is used." | S19 | 2026-09-11 | |
| Size limits | Max dimensions 8000×8000 px; max 10 MB (base64-encoded) per image on the Claude API (5 MB on Bedrock / Google Cloud); request size limit 32 MB; 100 images/request on 200k-context models, 600 otherwise; >20 images in one request triggers a stricter per-image limit (resize so neither side exceeds 2000 px, or stay ≤20 images). | S19 | 2026-09-11 | |
| Oversized-image handling | Downscaled by default; `tool_result` images for computer/browser use are rejected instead; set the image block's `transformations` field to get an error instead of a resize. | S19 | 2026-09-11 | |
| Ordering / compression tips | "Claude works best when images come before text." Lossy JPEG/WebP "can reduce latency by reducing the size of requests. However, this can introduce artifacts ... heavy JPEG compression can make text difficult to read." | S19 | 2026-09-11 | |
| Prompt caching activation | Two ways: **automatic** — top-level `"cache_control": {"type": "ephemeral"}` on the request ("applies the cache breakpoint to the last cacheable block and moves it forward"); **explicit** — `cache_control` on individual content blocks. Prefix order: `tools` → `system` → `messages`. | S20 | 2026-09-11 | Automatic caching returns 400 on the legacy Bedrock (Opus 4.6 and earlier) integration. |
| Minimum cacheable prompt length | **512** tokens: Claude Fable 5.1, Claude Mythos 5.1, Claude Opus 5, Claude Fable 5, Claude Mythos 5. **1,024**: Claude Opus 4.8, Sonnet 5, Sonnet 4.6, Sonnet 4.5, Opus 4.1, Opus 4, Sonnet 4. **2,048**: Claude Mythos Preview, Opus 4.7, Haiku 3.5. **4,096**: Claude Opus 4.6, Opus 4.5, Haiku 4.5. "Shorter prompts cannot be cached ... processed without caching, and no error is returned." | S20 | 2026-09-11 | Applies "on every platform where each model is available." |
| TTL options | Default 5-minute lifetime, refreshed free on each use; 1-hour via `"cache_control": {"type": "ephemeral", "ttl": "1h"}` (writes at 2× base input price). "Cache entries with longer TTL must appear before shorter TTLs" when mixing. Lifetime is measured from the start of the request that writes/reads. | S20 | 2026-09-11 | The page shows only the `"1h"` literal; a `"ttl": "5m"` literal is **UNVERIFIED** on this page (5 min is described as the default; usage exposes `cache_creation.ephemeral_5m_input_tokens` / `ephemeral_1h_input_tokens`). |
| Breakpoint limit | "You can define up to 4 cache breakpoints"; the automatic breakpoint "uses one of the 4 available breakpoint slots"; "If 4 explicit block-level breakpoints already exist, the API returns a 400 error". Lookback window is 20 blocks. | S20 | 2026-09-11 | |
| `cache_control` on system blocks | Yes: "System messages: Content blocks in the system array" are cacheable; documented example `"system": [{"type": "text", "text": "You are a helpful assistant.", "cache_control": {"type": "ephemeral"}}]`. Images in user turns are cacheable; adding/removing images invalidates the cache. | S20 | 2026-09-11 | |
| Usage fields | `usage.cache_creation_input_tokens`, `usage.cache_read_input_tokens`, `usage.input_tokens` (tokens after the last breakpoint); `usage.cache_creation.ephemeral_5m_input_tokens` / `ephemeral_1h_input_tokens`. | S20 | 2026-09-11 | |
| Cache pricing | 5m writes 1.25× base input; 1h writes 2×; reads 0.1× (0.025× on Claude Fable 5.1 / Mythos 5.1). | S20 | 2026-09-11 | |

## F. WebP encoding on macOS 14 via ImageIO

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| `UTType.webP` | "A type that represents a WebP image. The identifier for this type is `org.webmproject.webp`." Availability: macOS 11.0+, iOS/iPadOS 14.0+, Mac Catalyst 14.0+, tvOS 14.0+, watchOS 7.0+, visionOS 1.0+. C symbol `UTTypeWebP` has the same availability. | S21 | 2026-09-11 | This is only the availability of the type identifier; it says nothing about encode support. |
| `CGImageDestinationCopyTypeIdentifiers()` | "Returns an array of the uniform type identifiers that are supported for image destinations." Available macOS 10.4+. Companion `CGImageSourceCopyTypeIdentifiers()` for decodable types. | S22 | 2026-09-11 | Apple publishes **no per-format list** of what `CGImageDestination` can write; the documented mechanism is to query at runtime. |
| Decode support (context) | WWDC23 session 10122: "WebP was added to Safari 14 and macOS Big Sur." SDWebImage PR #3048 (2020): built-in ImageIO WebP decode from iOS 14 / macOS 11. | S23, S24 | 2026-09-11 | WWDC23 discusses web/Safari display formats only; it does not mention ImageIO encoding. |
| Encode support on macOS 14 Sonoma | **UNVERIFIED — conflicting anecdotal evidence, no Apple statement.** Against: StackOverflow 68802152 (2021-08-16, iOS 14 era) — `CGImageDestinationCreateWithURL(..., UTType.webP.identifier, ...)` fails with "unsupported file format 'org.webmproject.webp'", answer: "webp is not supported for encoding"; SDWebImage PR #3048 (2020): "Image/IO support does not supports encoding"; flutter_image_compress issue #369 (2026-07-15, iOS): "CGImageDestinationCreateWithData returns NULL (WebP is not authorable by ImageIO)". For: zenn.dev article (published 2024-11-20, OS version not stated) prints a `CGImageDestinationCopyTypeIdentifiers()` result that includes `"org.webmproject.webp"`. Apple Developer Forums: thread 732236 is about a mislabelled download (decode), and a forum search for "CGImageDestination webp" returned zero results in a real browser. | S24, forum attempts | 2026-09-11 | Nothing read today ties WebP *encoding* to macOS 14 specifically. |
| One-line check the owner can run on a macOS 14 machine | `echo 'import ImageIO; print((CGImageDestinationCopyTypeIdentifiers() as! [String]).contains("org.webmproject.webp"))' > /tmp/w.swift && swift /tmp/w.swift` — prints `true` if ImageIO on that OS can write WebP. Same with `CGImageSourceCopyTypeIdentifiers()` for decode. | S22 (documented API) | 2026-09-11 | If `false`, the frame encoder must use JPEG (`kCGImageDestinationLossyCompressionQuality`) or bundle libwebp. Note flutter_image_compress's finding that ImageIO cannot write EXIF into WebP even where the container is writable. |

## G. Cheap authenticated warm-up calls

| Fact | Value | Source | Verified on | Notes |
|---|---|---|---|---|
| Gemini `models.get` | `GET https://generativelanguage.googleapis.com/v1beta/{name=models/*}` — "Gets information about a specific Model such as its version number, token limits, parameters and other metadata." "The request body must be empty." Example: `curl https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash?key=$GEMINI_API_KEY`. `models.list`: `GET .../v1beta/models` (pageSize default 50). | S13 | 2026-09-11 | Response includes `inputTokenLimit`, `outputTokenLimit`, `supportedGenerationMethods[]`, `thinking`. |
| Auth header | `x-goog-api-key: YOUR_API_KEY` is the header form shown on the API-key page and in every REST sample on the guides (generateContent, interactions, files, countTokens); the `models.get` reference sample itself uses the `?key=` query form. | S14, S2, S12, S13 | 2026-09-11 | Header acceptance specifically on `models.get` is not shown → verify once. Also note (S14): "On September 2026: the Gemini API will reject requests from Standard keys" — use an auth key. |
| OpenAI list models | `GET https://api.openai.com/v1/models` with `Authorization: Bearer $OPENAI_API_KEY` — "Lists the currently available models, and provides basic information about each one such as the owner and availability." | S18 | 2026-09-11 | |
| "Warm-up" semantics | **UNVERIFIED / not documented** — neither provider describes these endpoints as connection pre-warming; any TLS/HTTP2 keep-alive benefit is an implementer inference. | — | 2026-09-11 | |

## Items still UNVERIFIED (and how to settle them)

1. **Per-model image token cost for `gemini-3.8-flash` and `gemini-3.5-flash-lite`** — docs give only a "Gemini 3 models" family table (1120/280/560/1120/2240). Settle: `POST /v1beta/models/{model}:countTokens` with one representative screenshot at each `mediaResolution` level per model, or read `usageMetadata.promptTokensDetails[]`.
2. **Reconciliation of the 258-token / 768×768-tile rule with the 1120-per-image Gemini 3 table, and any maximum tile count** — not model-qualified anywhere. Settle with the same `countTokens` sweep over sizes (e.g. 384², 768², 1536², 2560×1600).
3. **Implicit-caching minimum for `gemini-3.5-flash-lite`** — absent from the table (caching itself is "Supported" and priced). Settle: send an identical ≥4,096-token prefix twice within a minute and check `cachedContentTokenCount`; then bisect downward.
4. **Explicit-caching minimum token count** — page says "varies by model" with no separate number. Settle: `cachedContents.create` with a ~1k, ~2k, ~4k token body and observe 400 vs 200.
5. **Inline request limit 20 MB vs 100 MB** — two official pages disagree. Design to 20 MB; optionally test a 30 MB inline request once.
6. **Gemini TTFT/latency numbers** — only qualitative guidance exists. Measure.
7. **OpenAI Codex-model image token rules** — not in the vision tables. Query the image cost calculator (linked from S15) for the exact model ID, or measure `usage.input_tokens`.
8. **Anthropic `"ttl": "5m"` literal** — only `"1h"` is shown; 5 min is the documented default. Check the Messages API reference for the `cache_control.ttl` enum if you need to send it explicitly.
9. **WebP encode via ImageIO on macOS 14** — no Apple statement; conflicting third-party reports. Settle with the Swift one-liner in section F on a macOS 14 box (and on the minimum macOS version you ship).
10. **`x-goog-api-key` header on `GET /v1beta/models/{model}`** — reference sample uses `?key=`. One curl call settles it.

## Deltas vs the brief

| Brief assumption | Today's finding | Action |
|---|---|---|
| Gemini 3.x default image cost 1,120 tokens ("high") | **Confirmed** for the Gemini 3 family: `unspecified` (default) = 1120 = `high`. Not published per model. | Keep; verify per model with `countTokens`. |
| Per-part `mediaResolution` MEDIUM by default | **Wrong.** Default is `MEDIA_RESOLUTION_UNSPECIFIED`, billed like HIGH (1120). MEDIUM is 560, LOW is 280 and must be set explicitly (per-part `media_resolution: {level: ...}` next to `inline_data`, v1beta, "experimental"; or global `generationConfig.mediaResolution`). ULTRA_HIGH (2240) is per-part only. | Send an explicit level; treat the API-reference enum comments (64/256 tokens) as stale 2.5 numbers. |
| Implicit caching threshold ~1K tokens on Flash | **Wrong.** 4,096 tokens for 3.8/3.7/3.6/3.5 Flash (2,048 for 2.5 Flash). 3.5 Flash-Lite threshold unpublished. Cache hits do not count the per-request screenshot unless the prefix is identical, so keep the static system prompt ≥4,096 tokens and first. | Re-plan prefix sizing; consider explicit `cachedContents` (1 h default TTL) on generateContent if guaranteed savings matter. |
| OpenAI auto caching ≥ 1,024 tokens | **Confirmed for GPT-5.6 and later**; earlier models "vary by request settings". Hits in `usage.input_tokens_details.cached_tokens`; `prompt_cache_key` exists but is for accounting, not hit-rate. | Keep; also set `detail` explicitly — on gpt-6-astra / gpt-5.6-* the default `auto` behaves like `original` (full-resolution, 1.2× patches, rejects >30,000 patches). |
| Anthropic ≤ 1,568 px long edge | **Only for standard-tier models.** Claude 4.7 and later are high-resolution: 2,576 px long edge / 4,784 visual tokens, downscaled automatically. Token formula is now ⌈w/28⌉×⌈h/28⌉ (not w×h/750); resize before sending to cap cost. | Choose the target tier deliberately; pre-resize to ≤1,568 px if you want the cheaper standard-tier budget. |
| WebP encoding on macOS 14 uncertain | **Still uncertain**; Apple documents no per-format encode list. Both Gemini and OpenAI and Anthropic accept `image/webp`, so the format is safe on the API side if the encoder exists. | Run the `CGImageDestinationCopyTypeIdentifiers()` check on macOS 14; fall back to JPEG or bundled libwebp. |
